[package]
name = "orbitkv-executor"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Native Luminal execution boundary for OrbitKV."
license = "MIT"

[dependencies]
orbitkv = { path = "../core" }
thiserror = "2.0.17"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
pedantic = "deny"

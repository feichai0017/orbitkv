# Qwen3.8 kernels

This directory will contain qualified GDN, causal-convolution, and low-M FP8
kernel packages emitted or selected by the compiler. M0 deliberately contains
no placeholder CUDA implementation.

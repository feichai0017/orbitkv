[package]
name = "orbitkv-server"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Control-plane contracts for the OrbitKV inference engine."
license = "MIT"

[dependencies]
futures-core = "=0.3.31"
thiserror = "2.0.17"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
pedantic = "deny"

[package]
name = "orbitkv-ops"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "Backend-independent inference operation contracts and graph builders for OrbitKV."
license = "MIT OR Apache-2.0"


[dependencies]
orbitkv-compiler = { workspace = true }

[dev-dependencies]
candle-core = "0.9.2-alpha.1"
rand = "0.9.2"
